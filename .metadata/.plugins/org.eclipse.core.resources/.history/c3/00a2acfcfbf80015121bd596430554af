package com.tinkeracademy.projects;

public class Chat {

	public static void main(String[] args) {
		Chat chat = new Chat();
		javax.swing.SwingUtilities.invokeLater(new Runnable() {
			public void run() {
				chat.createAndShowGUI();
			}
		});
	}

	public void createAndShowGUI() {
		Window.show();
		Window.addLabel("Chat Application Developed By: Tinker Academy v1.0");
		Window.addTextArea("<Type in text to flip>", 10, 10, false);
		Window.addTextArea("<Type in text to flip>", 10, 10, true);
		Window.addButton("Send");
		
	}
}
